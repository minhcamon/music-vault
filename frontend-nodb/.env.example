# Google Drive API Key cho BFF Proxy (Serverless Functions trên Vercel hoặc Dev Server)
# KHÔNG sử dụng VITE_ prefix để đảm bảo API key không bị đóng gói (bundle) vào client-side code.
GDRIVE_API_KEY=your_google_drive_api_key_here
